# Flat, npm-style node_modules for maximum compatibility with the existing
# apps (Next.js + Prisma were built against a hoisted layout under npm).
node-linker=hoisted

# Don't fail the install on unmet peer deps — matches Relay's prior
# npm `legacy-peer-deps=true` behavior.
strict-peer-dependencies=false
auto-install-peers=true
