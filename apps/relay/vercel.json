{
  "buildCommand": "pnpm exec prisma generate && pnpm exec next build",
  "crons": [
    {
      "path":     "/api/cron/imap-sync",
      "schedule": "0 8 * * *"
    },
    {
      "path":     "/api/cron/billing-credits",
      "schedule": "0 9 * * *"
    }
  ]
}
